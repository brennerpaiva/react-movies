import { Link, useNavigate } from "react-router-dom";
import "./footer.css";

function Footer() {
  return (
    <footer>
      <span>Desenvolvido por Brenner Paiva | API: The Movie Database(TMDB)</span>
    </footer>
  );
}

export default Footer;
